services:
  - type: web
    runtime: node
    name: youtube-remote-control
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
